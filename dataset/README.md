Place the capture here as:

    dataset/two_lights/NNN_rgb.png      # 640x480 BGR
    dataset/two_lights/NNN_depth.npy    # float32 metres, aligned to RGB
    dataset/two_lights/intrinsics.json  # already provided

(RealSense D435-family, depth aligned to colour.)
